# Use Structured Suggestions with Review Narratives

Edit Suggestions will use a strict structured schema as the source of truth for review, validation, persistence, and apply-ready Saved Updates. Review Narratives may be generated or displayed to improve user comprehension, but production behavior must not depend on parsing prose because free-form text is harder to test, diff, edit, and persist reliably.
